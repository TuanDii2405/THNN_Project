fastapi==0.116.1
uvicorn[standard]==0.35.0
numpy==2.2.6
opencv-python==4.12.0.88
google-cloud-vision==3.10.2
easyocr==1.7.2
itsdangerous==2.2.0
# Face auth stack on Windows (install manually to avoid dlib source build):
# pip install dlib-bin==20.0.1
# pip install face-recognition-models Pillow
# pip install face-recognition --no-deps
# EasyOCR is required for CCCD OCR fallback when Google Vision is not configured.
